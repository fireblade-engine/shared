{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "args": [],
            "cwd": "${workspaceFolder:shared}",
            "name": "Debug shared-actions",
            "program": "${workspaceFolder:shared}/.build/debug/shared-actions",
            "preLaunchTask": "swift: Build Debug shared-actions"
        },
        {
            "type": "lldb",
            "request": "launch",
            "args": [],
            "cwd": "${workspaceFolder:shared}",
            "name": "Release shared-actions",
            "program": "${workspaceFolder:shared}/.build/release/shared-actions",
            "preLaunchTask": "swift: Build Release shared-actions"
        }
    ]
}